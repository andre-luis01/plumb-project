import { useEffect, useState } from "react"

function Form() {
    const [questionCounter, setQuestionCounter] = useState<number>(0)
    useEffect(() => {
        
    }, [questionCounter])

    return ()
}